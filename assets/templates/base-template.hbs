<html>
  <head>
    <title>{{title}}</title>
  </head>
  <body>
    <h1>{{header}}</h1>
    <ul>
      {{#each pullRequests}}
        <li>{{this.title}}</li>
      {{/each}}
    </ul>
  </body>
</html>